(def fib
 (fn [n]
  (if (<= n 2)
   1
   (+ (fib (- n 1)) (fib (- n 2)))))
 )

(def
 dump (fn [m]
     (let [f (first m)]
      (if f
       (do 
        (println f)
        (dump (rest m)))
       (println "done")))))


